<!DOCTYPE html>
<html>
  <head>
    <title>SjiDemoProject</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="stylesheet" href="http://cdn.jsdelivr.net/npm/@mdi/font@6.9.96/css/materialdesignicons.min.css">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <%= render 'application/navbar' %>
    <div class='container'>
      <% flash.each do |type, message| %>
        <div class="auto-hide alert <%= bootstrap_class_for(type) %>">
          <%= message.html_safe %>
        </div>
      <% end %>
      <%= yield %>
    </div>
  </body>
</html>
